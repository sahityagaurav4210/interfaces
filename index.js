const CannonPixma = require("./Printer");
const IPrinter = require("./interfaces/IPrinter");

let printer = new IPrinter(); // will throw an error
// let pixma = new CannonPixma(); // Works fine

// pixma.print("Hello world...");
